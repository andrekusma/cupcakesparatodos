# Use uma das duas abordagens abaixo:
# 1) URL completa (Heroku/Railway etc.)
# DATABASE_URL=postgres://usuario:senha@localhost:5432/cupcake_db

# 2) Variáveis separadas
PGHOST=localhost
PGPORT=5432
PGDATABASE=cupcake_db
PGUSER=postgres
PGPASSWORD=postgres

# Servidor
PORT=3000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000